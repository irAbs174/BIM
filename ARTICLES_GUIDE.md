# 📝 راهنمای نمایش مقالات

## مشکل
مقالات در آرشیو و صفحات جزئیات نمایش داده نمی‌شوند.

## دلیل اصلی
دیتابیس خالی است و هیچ مقاله‌ای برای نمایش وجود ندارد.

## حل سریع (3 مرحله)

### 1️⃣ سرور بکند را شروع کنید
```bash
cd backend
python3 main.py
```
سرور باید در `http://localhost:8000` اجرا شود.

### 2️⃣ مقالات را seed کنید
```bash
# روش 1: استفاده از اسکریپت
./seed-articles.sh

# یا روش 2: استفاده از curl
curl -X POST http://localhost:8000/api/articles/seed-demo
```

### 3️⃣ فرانتند را شروع کنید
```bash
npm run dev
```

## نتیجه انتظاری
بعد از این مراحل:
- ✅ مقالات در صفحه اصلی نمایش داده می‌شوند
- ✅ آرشیو مقالات تمام مقالات را نمایش می‌دهد
- ✅ بر روی هر مقاله کلیک کنید تا جزئیات را ببینید

## API Endpoints

### مقالات عمومی:
```
GET /api/articles - لیست تمام مقالات منتشرشده
GET /api/articles/{id_or_slug} - جزئیات یک مقاله
GET /api/articles/tags/all - لیست تمام برچسب‌ها
POST /api/articles/seed-demo - Seed مقالات نمونه (توسعه)
```

### مقالات Admin:
```
POST /api/articles (احراز هویت مورد نیاز) - ایجاد مقاله
PUT /api/articles/{id} (احراز هویت مورد نیاز) - ویرایش مقاله
DELETE /api/articles/{id} (احراز هویت مورد نیاز) - حذف مقاله
```

## اطلاعات مقاله

هر مقاله شامل اطلاعات زیر است:
- `title_en` / `title_fa` - عنوان انگلیسی و فارسی
- `slug` - شناسه URL
- `summary_en` / `summary_fa` - خلاصه
- `content_en` / `content_fa` - محتوای کامل
- `category` - دسته‌بندی (BIM, Surveying, Technology, News)
- `image_url` - URL تصویر
- `tags` - برچسب‌ها (جدا شده با کاما)
- `author` - نویسنده
- `is_published` - منتشرشده؟
- `publish_date` - تاریخ انتشار

## حل مسائل عام

### مقالات هنوز نمایش داده نمی‌شوند؟
1. بررسی کنید که سرور بکند اجرا شود: `http://localhost:8000/api/articles`
2. بررسی کنید که پاسخ خالی نیست
3. کنسول مرورگر را باز کنید (F12) و خطاها را بررسی کنید

### خطا: "Cannot POST /api/articles/seed-demo"
سرور بکند اجرا نمی‌شود. مرحله 1 را دوبار انجام دهید.

### مقالات منتشرشده نمایش داده نمی‌شوند؟
فقط مقالاتی با `is_published: true` نمایش داده می‌شوند. Admin Panel را باز کنید و بررسی کنید.

## فایل‌های مرتبط
- [ARTICLES_FIX.md](./ARTICLES_FIX.md) - توضیح فنی
- [backend/app/routers/articles.py](./backend/app/routers/articles.py) - Endpoints
- [src/components/ArticlesArchive.vue](./src/components/ArticlesArchive.vue) - صفحه آرشیو
- [src/components/FeaturedArticles.vue](./src/components/FeaturedArticles.vue) - مقالات برجسته
